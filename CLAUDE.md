# Frontend memory — auto-loaded every session

# Shared workspace agent guide + full context/ pack (one level up).
@../AGENTS.md

# Local frontend build/design log (detailed UI conventions; NOTE: its data-flow
# section is stale — the shared pack above is authoritative for data flow).
@AGENTS.md
